# 安装依赖指南

## 📋 前置要求

### 1. 安装 Node.js

项目需要 Node.js 来运行。如果还没有安装，请按照以下步骤安装：

#### Windows 安装 Node.js

1. **下载 Node.js**
   - 访问 Node.js 官网: https://nodejs.org/
   - 下载 LTS 版本（推荐，例如 v20.x.x）
   - 选择 Windows Installer (.msi) 64-bit

2. **安装 Node.js**
   - 运行下载的 .msi 安装程序
   - 按照安装向导完成安装
   - **重要**: 确保勾选 "Add to PATH" 选项

3. **验证安装**
   - 打开新的 PowerShell 或命令提示符窗口
   - 运行以下命令验证：
   ```bash
   node --version
   npm --version
   ```
   - 如果显示版本号，说明安装成功

#### macOS 安装 Node.js

**方式一：使用 Homebrew（推荐）**
```bash
brew install node
```

**方式二：使用官方安装包**
- 访问 https://nodejs.org/
- 下载 macOS Installer (.pkg)
- 运行安装程序

#### Linux 安装 Node.js

**Ubuntu/Debian:**
```bash
# 使用 NodeSource 仓库
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

**CentOS/RHEL:**
```bash
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs
```

### 2. 验证 Node.js 安装

安装完成后，重新打开终端窗口，运行：

```bash
node --version
npm --version
```

应该显示版本号，例如：
```
v20.10.0
10.2.3
```

## 📦 安装项目依赖

### 方式一：一键安装所有依赖（推荐）

在项目根目录运行：

```bash
npm run install:all
```

这个命令会：
1. 安装根目录依赖
2. 安装后端依赖
3. 安装前端依赖

### 方式二：分别安装依赖

#### 1. 安装根目录依赖

```bash
cd "E:\Future teller Project"
npm install
```

#### 2. 安装后端依赖

```bash
cd backend
npm install
```

#### 3. 安装前端依赖

```bash
cd ../frontend
npm install
```

## 🔍 验证安装

安装完成后，检查 `node_modules` 目录是否存在：

```bash
# 检查根目录
Test-Path "node_modules"

# 检查后端
Test-Path "backend\node_modules"

# 检查前端
Test-Path "frontend\node_modules"
```

所有应该返回 `True`。

## 🚀 安装完成后的下一步

### 1. 启动 MongoDB

确保 MongoDB 服务正在运行：

**Windows:**
```bash
mongod
# 或
net start MongoDB
```

### 2. 启动开发服务器

```bash
npm run dev
```

### 3. 访问应用

- **前端**: http://localhost:3000
- **后端API**: http://localhost:5000/api

## 🐛 常见问题

### 问题1: 'node' 不是内部或外部命令

**原因**: Node.js 未安装或未添加到 PATH

**解决方案**:
1. 重新安装 Node.js
2. 确保勾选 "Add to PATH" 选项
3. 重启终端窗口
4. 如果还是不行，手动添加 Node.js 到系统 PATH

### 问题2: npm install 失败

**错误**: 网络错误、权限错误等

**解决方案**:
1. **使用国内镜像（推荐）**:
   ```bash
   npm config set registry https://registry.npmmirror.com
   ```

2. **清除缓存**:
   ```bash
   npm cache clean --force
   ```

3. **删除 node_modules 和 package-lock.json**:
   ```bash
   Remove-Item -Recurse -Force node_modules
   Remove-Item package-lock.json
   npm install
   ```

4. **使用管理员权限运行**:
   - 右键点击 PowerShell
   - 选择 "以管理员身份运行"
   - 再次执行安装命令

### 问题3: 安装速度慢

**解决方案**:
1. 使用国内镜像:
   ```bash
   npm config set registry https://registry.npmmirror.com
   ```

2. 使用 yarn（如果已安装）:
   ```bash
   yarn install
   ```

3. 使用 cnpm:
   ```bash
   npm install -g cnpm --registry=https://registry.npmmirror.com
   cnpm install
   ```

### 问题4: 权限错误（macOS/Linux）

**解决方案**:
```bash
# 不要使用 sudo 安装项目依赖
# 如果遇到权限问题，修复 npm 权限
mkdir ~/.npm-global
npm config set prefix '~/.npm-global'
```

## 📝 安装步骤总结

1. ✅ 安装 Node.js（如果未安装）
2. ✅ 验证 Node.js 安装
3. ✅ 安装项目依赖: `npm run install:all`
4. ✅ 验证安装
5. ✅ 启动 MongoDB
6. ✅ 启动开发服务器: `npm run dev`

## 🔗 相关链接

- Node.js 官网: https://nodejs.org/
- npm 文档: https://docs.npmjs.com/
- 项目 README: [README.md](./README.md)
- 快速启动: [QUICK_START.md](./QUICK_START.md)

## 💡 提示

- 安装依赖可能需要几分钟时间，请耐心等待
- 如果遇到网络问题，可以使用国内镜像加速
- 建议使用 Node.js LTS 版本（长期支持版本）
- 安装完成后，TypeScript 类型错误会自动消失


